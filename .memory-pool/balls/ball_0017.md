v1.5.93 新增全局记忆池功能：

- 存储位置：~/.ccweb/memory-pool/（pool: "global", lambda: 0.99, active_capacity: 40）
- 全局 t 按日历天数递增（UTC day diff），不受 sync 频率影响
- 项目 init/upgrade 自动注册到 sources.json
- sync 流程：收集所有项目球→增量合并→孤儿检测→门控写入（balls 先写，pool.json 最后写）
- import 流程：预览→选择性导入，B0 归一化（min(全局浮力, 项目最低活跃B*1.5)），links 只映射同批+已导入球
- 并发锁 syncInProgress + try/finally 保证释放
- 前端：同步全局 / 引用全局 按钮，导入预览面板，全局徽章
- 首页 Brain 图标打开全局气泡可视化
