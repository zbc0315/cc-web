v1.5.94: 记忆池并发安全加固。新增 pool-lock.ts Promise-chain 互斥锁，所有 pool.json 写操作必须在 withPoolLock 内执行。修复 8 处路由 try/catch→.catch() 模式（C-2）、GET /surface 补偿 tick 加锁（H-1）、Stop hook tickPool 加锁（H-2）、全局 surface 写回 pool.t（H-3）、POST /init 和 /upgrade 加锁（H-3）、syncToGlobal 加锁（M-1）、buildSurface 跳过 diameter=0 鬼球（M-1）、POST /balls 验证 links 存在性（M-2）、POST /balls 内容长度限制 summary≤500/content≤100KB（L-6）。