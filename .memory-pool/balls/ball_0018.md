记忆池 v2 迁移完成（2026-04-04）：

- state.json + index.json 合并为 pool.json
- 球文件去除 YAML frontmatter，改为纯 markdown
- 浮力动态计算（后端 computeBuoyancy），不再存储在文件中
- 新增 permanent 标记（跳过衰减）、global_ball_id（全局引用溯源）
- 旧文件备份为 .v1bak
