cc-web 核心架构：Browser(React/Vite) → REST API + WebSocket → Express(:3001) → TerminalManager → node-pty($SHELL -ilc "claude") → Claude CLI。PTY-first 设计，所有 Claude Code 功能原生可用。无数据库，纯 JSON 文件存储。前端自动端口切换(3001-3020)，localhost 免 JWT 认证。前后端代码分别在 backend/src/ 和 frontend/src/，shadcn/ui(zinc theme) + Tailwind。
